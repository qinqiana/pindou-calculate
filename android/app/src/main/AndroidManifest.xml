<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local assets and Android document grants need no network/storage permission. -->
    <application android:label="豆计" android:icon="@drawable/app_icon"
        android:theme="@style/AppTheme" android:allowBackup="false"
        android:usesCleartextTraffic="false" android:supportsRtl="true">
        <activity android:name=".MainActivity" android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
