# 拼豆库存与图纸消耗软件需求书（讨论稿 v0.2）

日期：2026-09-15。状态：待确认；本文中的“建议”不是已经拍板的业务规则。

## 1. 产品目标与范围

帮助个人拼豆爱好者回答三个问题：每种颜色还剩多少颗；一张图纸分别需要多少颗；按现有库存制作后，哪些颜色不足、预计剩余多少。首版以个人使用、离线优先的轻量软件为目标；后续保留迁移到小程序的可能，不在首版承诺账号、云同步或多人协作。

首版的成功标准：使用用户提供的 MARD 221 色卡录入库存后，能对一张受支持的图纸生成逐色需求、总需求、库存缺口和预计余量；确认制作后能准确扣减库存，并可追溯和撤销误操作。

## 2. 用户与核心场景

用户为持有多色拼豆、按图纸制作作品的个人。典型流程：初始化色卡 → 盘点并录入库存 → 导入图纸或手工录入用量 → 核对颜色映射与数量 → 查看能否制作及补货清单 → 确认制作并扣减 → 后续入库、盘点或更正。

“库存”指当前实际可用的颗数；“图纸需求”指该作品各颜色所需颗数；“预计余量”仅用于预览，不等于已扣减库存。用量和库存均以非负整数颗为单位。色号必须与色卡版本绑定，不能仅凭屏幕显示的 RGB 值认定两颗豆为同一色。

## 3. 首版功能需求

### 3.1 色卡管理

- 首版采用用户提供的“MARD 221 标准版拼豆色卡示意图”的色号体系，共 9 个色系、221 个色号；每色至少有唯一色号、所属色系、展示色值和色卡版本。图中没有逐色 HEX/RGB 数值，展示色值数据仍须从可靠原始表取得并核对；不能直接从截图取样当作权威色值。
- 色号范围：A1–A26（黄橙系，26）；B1–B32（绿色系，32）；C1–C29（蓝青系，29）；D1–D26（蓝紫系，26）；E1–E24（粉玫系，24）；F1–F25（红色系，25）；G1–G21（棕肤系，21）；H1–H23（黑白系，23）；M1–M15（大地系，15）。合计 221。以上名称和数量依据用户提供的示意图，不代表已经核验官网版本。
- 色卡只作为匹配、展示和校验的依据；不自动把其他品牌的近似颜色视为同一库存。
- 用户表示原始图纸不会使用 MARD 221 色卡外色号；识别出的色卡外色号不得进入正式逐色用量，但必须在确认页面显示被拒绝项及原始识别内容，不得静默丢弃。用户允许看过提示后不处理该项、继续确认；此时该项仍不计数，存在有效色号被误读后漏算的风险。确认页面必须清楚显示“未计入”的色号和数量，而不能把它混入正式总数。

### 3.2 库存管理

- 按色号查看当前库存、低库存提示；支持逐色录入、批量盘点、采购入库、制作消耗及库存更正。
- 每次实际库存变化记录时间、色号、变动数量、原因、关联作品/图纸及变动前后数量；误扣减可通过反向记录撤销，不直接抹除历史。
- 不允许库存变成负数。数量不足时仍能查看缺口，但确认扣减必须明确处理方式（首版建议阻止扣减）。
- 建议支持 CSV 导入/导出及本地备份，避免换设备或升级时丢失盘点数据；具体格式在色卡确定后定义。

### 3.3 图纸与用量

- 用户提供的典型样例为“豆画 / Mard (148)”截图：29 列 × 16 行坐标网格，制作格内直接标 MARD 色号，底部有逐色数量图例。图例为 C2=18、C12=3、C16=11、C17=12、C19=7、C29=18、H2=14、H3=5、H7=60；图例求和 148 颗。该样例显示 H2 白色格也需要计数，而未标色号的棋盘格不应仅因外观接近白色就算作 H2。
- 每张图纸保存名称、来源文件/备注、尺寸、色卡版本、逐色需求及处理状态。
- 首版输入确定为 PNG/JPG 图纸截图；用户表示日常图纸每张都有底部逐色数量图例。首版以图例为用量的主要数据源，不要求自动逐格复算。导入后必须展示识别出的各色数量和总数，允许逐色修改、删除错误项或新增漏项；总数应由确认后的逐色数量求和。用户确认后才可用于库存对比和扣减，截图自动识别不得绕过确认步骤。
- 手工输入每色颗数可作为候选兜底流程；结构化图纸导入和普通照片转网格留待后续评估，不作为当前已确认的首版输入。
- 样例中每个明确标有有效色号的制作格对应一颗豆；未标色号的格子不计数，包括未标号的棋盘格背景。首版不依赖网格 OCR 来生成用量；确认后的逐色数量之和为总需求。若能读到截图标题总数，且其与逐色求和不一致，必须显示差值并要求用户再次明确确认；允许确认后的逐色数量作为库存计算依据，不强制采用标题数字。
- 导入前显示预览、未识别色号和统计结果；用户确认后才保存需求。若图纸色卡与库存色卡不一致，需提示并手工确认映射。
- 同一图纸可以多次预估，但每次实际制作应有独立制作记录，避免重复扣减。

### 3.4 消耗预测与扣减

对每个色号 c：需求量 `need[c]`；当前库存 `stock[c]`；缺口 `max(need[c] - stock[c], 0)`；预计余量 `stock[c] - need[c]`（不足时显示负值或以“缺口”呈现，不写入负库存）。只统计图纸里出现的颜色，同时提供全色库存视图。

系统显示总需求、涉及颜色数、足量/不足颜色、逐色缺口及可导出的补货清单。预估不改变库存；用户明确点击“确认制作”且通过库存校验后，逐色一次性扣减并产生一条制作记录。重复点击或重试不得造成重复扣减。

## 4. 首版页面/模块建议

色卡/库存总览（搜索色号、筛选不足）；库存录入与变动记录；图纸列表与导入预览；单张图纸的逐色需求及库存对比；确认制作与历史记录；数据备份/导出。移动端后续可复用业务规则，但界面需重新适配触屏与小程序文件选择限制。

## 5. 数据概念与关键约束

| 对象 | 最小字段 | 约束 |
| --- | --- | --- |
| 色卡版本 | 版本标识、来源、启用时间 | 一个版本内色号唯一；旧图纸保留原版本 |
| 颜色 | 色号、名称、展示色值、色卡版本 | 展示色值不作为库存主键 |
| 库存余额 | 色卡版本、色号、颗数 | 颗数为非负整数 |
| 库存流水 | 时间、色号、变动值、原因、关联记录 | 真实变动可追溯 |
| 图纸 | 名称、来源、尺寸、色卡版本、状态 | 原始文件可选；需求可复核 |
| 图纸用量 | 图纸、色号、颗数 | 颗数非负；总量为逐色求和 |
| 制作记录 | 图纸、确认时间、扣减快照、状态 | 同一制作请求最多扣减一次 |

## 6. 非功能要求与验收样例

- 计算应可重复验证：同一图纸、同一色卡与同一库存快照，输出一致；库存发生变化时预估实时更新。
- 导入失败不能改动库存；确认扣减须整体成功或整体失败，不能只扣其中几个颜色。
- 本地数据需有明确的导出与恢复途径；文件导入要限制格式和尺寸并给出可理解的错误信息。
- 无障碍基础：颜色不作为唯一信息，始终显示色号与数字。

验收样例：库存 A1=100、B1=10；图纸有效格 A1=80、B1=15、空白=5。结果为总需求 95、A1 余量 20、B1 缺口 5；预览不改变库存，首版建议因 B1 不足而阻止确认制作。补入 B1=5 后确认制作，余额 A1=20、B1=0；再次提交同一制作请求不再扣减；撤销产生反向流水并恢复余额。

## 7. 分阶段规划

- 阶段 1（MVP）：核验并导入 MARD 221 色卡数据、库存录入/流水、PNG/JPG 截图导入及用量确认、用量对比、缺口与确认扣减、数据导出/恢复。截图识别范围和失败兜底仍待明确。
- 阶段 2：普通图片转网格、人工校色、尺寸与调色策略、多个品牌色卡/映射、采购统计。
- 阶段 3：小程序适配、账号与多设备同步（若确有需求）、云端备份；先确认数据迁移、隐私与成本。

## 8. 待用户确认的产品决策

1. MARD 221 色卡的逐色 HEX/RGB 原始数据和具体版本从哪里取得？当前图片只可确认色号范围与分组，不能当作精确色值表。
2. 首版运行形态是 Windows 桌面、手机网页，还是先用表格/网页验证？是否需要完全离线？
3. 图纸输入已确定为 PNG/JPG 截图，且每张都有底部用量图例；首版以图例为主，不必逐格自动复算。用户可新增漏项；无效色号不进入正式用量，但必须显示被拒绝项，用户可不处理而继续；标题总数冲突必须提示并再次明确确认。该整套规则尚待用户核对是否理解一致。
4. 一包豆按颗数记账，还是先按包/估算数量记账？盘点与消耗是否允许近似值？
5. 库存不足时禁止确认制作，还是允许部分扣减/负库存？同一图纸多次制作如何记录？
6. 是否区分豆子尺寸、品牌、批次与损耗？若存在，这些维度可能影响库存主键和用量。

以上决策确认后，才能把文件格式、页面细节及验收标准从讨论稿改成可开发的正式需求书。
