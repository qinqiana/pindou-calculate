# 使用 Android 系统容器交付自用安装包

2026-09-23 用户确认去掉 DCloud 手机运行库与离线 AppKey，优先使用 Android 自身能力，尽量不引入第三方。现有 Vue/uni-app 页面继续编译为随包网页，由系统 WebView 展示；文件选择、图片解码、SQLite 事务和相册/下载输出通过少量 Android API 接口完成，不新增容器 SDK。

复用账本及备份 v2 数据合同，原生存储提交成功后才更新可见状态。现有 DCloud 开发基座与独立应用属于不同沙箱，通过用户导出/恢复备份接续数据；不读取或覆盖其他应用私有存储。自用包由用户保管的本地密钥签名，不包含私钥、凭据或个人账本。
