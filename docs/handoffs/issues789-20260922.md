# #7 / #8 / #9 交接：Android 联调未完成

交接时间：2026-09-22 12:55 左右（香港时间）。用户要求“现在测试到哪了？先下个 handoff”，因此暂停实施与手机测试，先交接。**尚未提交、推送或创建本轮 PR，不能把三票标记完成。**

## 目标、授权与接续边界

- 用户最新完整实施要求：“做7 8 9吧，不是安卓开着吗 没问题再pr”。完成三票、在现有 Android 真机验证关键路径后再创建 PR；已授权必要 commit/push/PR。这取代此前阶段性的“不提交、不推送”。交接后按用户下一条指令接续，不自行恢复测试。
- 根规格为 `SPEC.md`；领域定义为 `CONTEXT.md`；识图专项为 `docs/specs/image-recognition.md`。不要因换会话重做已验证实验或改变算法验收标准。
- UI 已确认方向：已录入库存优先，全部 221 色按组展开，搜索覆盖全部色号；不是只增加首屏行数。用户要求参考 Apple 设计规范，**尚未做用户审美验收**。
- Android 调试已授权，不操作微信、不改变手机安全设置、不上传原图、不接付费服务。正式签名 APK 和公开发行仍未完成，调试基座不冒充发行包。
- 主会话负责算法与最终结论。当前已确认的低一级实施模型是 Luna，禁止用于 QA；没有符合边界的独立 QA 模型，所以目前是 **自行复核，未做独立 QA**。不要自行推定其他模型级别。
- 不做哈希/指纹登记；原文件验证用直接字节相等即可。保护全部现有改动与用户账本，不 reset/clean。

## 当前 Git 与工作目录

- 主目录：`/home/kenbattle/apps/pindou-calculate`。
- 分支：`codex/issue-8-local-legend`，基于已合并的主线；本轮大量修改和新文件尚未提交。之前分支整合已完成，勿重复清理历史分支。
- 范围包括：#7 WebP/原生文件适配、#8 0.8 算法和历次验证报告、#9 核对编辑与来源持久化、已授权库存 UI、Linux 构建配置、文档/测试。逐项看 `git status --short`，不要当作无关脏文件删除。
- 构建产物、27 MiB 左右离线运行时下载和设备测试素材为忽略项或 `/tmp` 文件，不提交安装包、原图副本或本地配置。
- 已有 PR #6 属历史合入；本轮没有新 PR。

## 已通过的实际检查

| 项目 | 已取得的证据 | 限制 |
| --- | --- | --- |
| 18 张原图导入 | 手机完整解码、创建图纸、确认手工用量，18/18；1 PNG、4 JPEG、13 WebP | 读取测试应用内逐字节复制的原文件，不等于每张经过系统选择器 |
| 手机离线算法 | 148 原图九色完全一致：C2=18、C12=3、C16=11、C17=12、C19=7、C29=18、H2=14、H3=5、H7=60；算法约 7.7 秒，完整调用约 11.9 秒 | 不是所有图都完整识别；partial 风险仍需保留 |
| 实际编辑页 | 自动填入九色；打开/关闭原图放大后输入保留；C12 从 3 改为 4，实际 UI 确认保存 149 | 保存了原候选合计 148、最终 149、legend 来源、modified=true；原候选标题为 null，不要误报标题 148 |
| 不扣库存 | 上述确认后库存总数 0、变动流水 0 | 使用隔离账本 |
| 公共下载导出 | 实际导出 `/sdcard/Download/豆计/pindou-acceptance-789.json`，4,585,013 字节；ADB 取回后完整 JSON 可读，formatVersion=2、appVersion=0.2.0、22 图纸、seq=42、epoch=1、movements=0，保留自动确认 149 | 尚未从系统选择器选该文件恢复 |
| 系统相册 | 原生保存返回成功；MediaStore 查到 `pindou-acceptance-789.png`，`DCIM/Camera/`，1080×1440 | 是朋友圈模板保存链路；最新 PNG 像素/视觉、小红书模板和产品分享页按钮还需补验 |
| 自动化 | Linux 算法 23 项通过；上次完整应用回归 84 项通过；后续编辑相关 28 项、最新文件适配 20 项通过 | 原子确认尚未整合；不能将旧 84 项当最新全量结果 |

证据位置：

- 正式记录：`docs/acceptance/issues789-20260922/README.md`。
- 已保存逐图结果：`docs/acceptance/issues789-20260922/android-originals.json`。
- 已保存确认记录：`docs/acceptance/issues789-20260922/android-confirmed-edit.json`。
- 最新导出与相册摘要：同目录 `android-file-output.json`。
- 手机/临时原始证据：`/tmp/pindou-issues789-20260922/device-test/` 下 `algorithm-148-result.json`、`confirmed-149.json`、`recognition-148.png`、`native-batch-progress.json`、`public-backup.json`。
- 测试日志：`/tmp/pindou-issues789-20260922/algorithm-tests.log`、`app-tests-current.log`、`editor-tests.log`、`fs-tests.log`。

**受限沙箱的 node --test 曾只报告文件通过而未运行真正子测试。** 必须在获准执行环境运行，检查具名子测试和实际数量；必要时 `--test-isolation=none`。同样，网页预览不能替代 Android 运行。

## 主目录已经实现的内容与真机修复

1. `app/src/platform/image.ts`：完整原生解码、256 像素 PNG 缩略图、透明白底及 EXIF 方向；Native.js 静态常量改用 `importClass(...).字段`。URI 仅在 WeakMap 中随内存原图存活，允许 ImageDecoder 直接读取授权原文件；S04 准备由约 19.6 秒降至 4.7 秒，不持久保存原图/URI。
2. `app/src/platform/fs.ts`：Android OPEN_DOCUMENT 原字节读图；分享图改用 nativeObj.Bitmap（FileWriter.write 不支持浏览器 Blob）；Android 29+ 用 MediaStore Downloads 发布导出，失败只删除新建未完成项。旧 `_downloads/` 实际是应用私有目录，已替换。Android 28 以下公共目录分支仅有测试替身，当前没有旧系统真机。
3. `app/components/RecognitionRunner.vue`、`app/static/recognition/worker.mjs`：同一 Python 算法在 module Worker 中离线运行，随包 Pyodide/NumPy/OpenCV/Pillow。仅接受原图与静态 MARD221 色卡，不接账本答案；无远程回退。取消/离开/编辑/超时终止 Worker。
4. 两个真实桥接故障已修：页面识别延后到 onReady，避免 `$ownerInstance` 未就绪；DCloud callMethod 会丢失对象里的 null 字段，事件改成整包 JSON 字符串跨桥，父页再解析，保住 confirmedVersion/titleTotal。
5. `app/src/recognition/result.ts`：请求、原图会话、图纸、编辑版本、确认版次、epoch 全部匹配才接受结果；候选验证、区域疑点、partial 覆盖风险。`tests/recognition-session.test.ts` 与编辑 VM 测试覆盖旧结果/编辑竞态等。
6. `app/pages/pattern/edit.vue`：原图放大、候选采用/差异、逐项编辑、风险确认、30 秒超时、独立手工兜底。失败不能自动确认零用量；重新打开自动结果仍保留自动来源。详情保留来源、人工改动、原始完整性及风险，CSV 导出有风险提醒。
7. `ledger/provenance.ts`、types/store/operations：识别来源、原候选、最终用量、修改事实、疑点及确认持久化；备份 v2，合法 v1 手工数据迁入，未来/损坏/悬空版次拒绝恢复。旧制作按 usageVersion 关联旧确认版本。
8. 应用版本 0.2.0、manifest versionCode 200；编译器固定 5.26，依赖锁文件与 Vite 配置已加入。`uniStatistics` 已禁用。
9. 库存页已改为已录入优先、九组展开、全局搜索；图标及设计证据在主目录。编辑页刚压缩原图高度/按钮布局并去掉用户流程里的算法版本号，新布局尚需亲自看截图。

## 必须先处理的代码缺口：整体确认要原子保存

主目录编辑页 `confirmAll` 仍先 `updatePatternMeta`，后 `confirmUsage`。标题差异/用量失败时，元数据可能已先保存；重新选图也还没有把新缩略图同最终确认一起提交。这是已识别、尚未修完的缺口。

已约定最小修法（无需重新向用户问技术细节）：

```ts
confirmUsage(..., {
  // 现有用量/来源字段保留
  patternMeta?: {
    name: string
    sourceNote: string
    sizeNote: string
    imageBytes?: Uint8Array // 只在本页成功重新选图时传入
  }
}, token)
```

- 所有校验与可选图片完整解码在事务前完成；同一次 `this.write` 保存元数据、可选新缩略图/尺寸和 ConfirmedUsage，只生成一个 confirm 操作。
- canonical 放规范化元数据及生成的缩略图/尺寸，不放原图字节、URI、路径、哈希。未传 patternMeta 时保持既有手工请求 canonical 兼容。
- 编辑页加入 imageChanged 标志，初始 handoff 原图不算重选；删除单独 updatePatternMeta 调用。
- 验证成功；标题差异/风险/坏图/落盘失败均无局部保存；重复请求换元数据/图片被拒绝。不要新增图片 MIME、历史缩略图快照、默认补全框架或额外备份迁移。

**子代理工作区暂不能直接复制/合并：**

- `/tmp/pindou-issues789-20260922/provenance-worktree`，分支 `codex/issue-9-provenance`，Luna 实施代理 `/root/issue9_provenance`。
- 较早完成的 provenance 代码已整合到主目录。本次 follow-up 一度偏离最小范围，新增 replacement、imageMime、历史快照和 fillDefaults；主会话已明确要求撤回这些扩展。交接时代理被中断，精简未完成。
- 最新 diff 仍有不应存在的备份改动，引用已删除的 `fillPatternDefaults/fillConfirmedDefaults`；patternMeta 还未支持 imageBytes，类型校验还有 `.trim()` 先于 typeof 的问题。**不把这个工作区当可用成品，不覆盖主目录 types/store/operations。** 接续者只拿有用的局部思路，完成上述最小实现。

## 尚未完成的手机检查（按优先级）

1. 整合上述原子保存，重新运行受影响测试、最终全量应用测试和 app 构建。
2. 实际系统选图原字节一致性。前次用户“显示 running”后的结果其实是旧测试页默认 decode 命令，**不能作为系统选图通过证据**。当前临时测试页已改成专用按钮，避免命令输入未提交。小米阻止远程点击系统文件，需要时只请用户做一次明确的文件选择。
3. 系统文件选择器读取备份 → 整体恢复；同一真机完成 v2 往返与至少一次 v1 迁入，损坏/未来版本失败保护。`pickViaAndroidIntent` 仍使用旧的 Java 直接方法调用，可能要 importClass/invoke 适配，先实测再修。
4. 强制结束本应用进程并重新打开，检查 149 用量、148 原候选、来源/风险、历史版次仍存在，库存未改变。
5. S03 部分图例真实核对：无风险确认阻止保存、编辑后撤销风险确认、原图疑点定位、确认后详情持续提示，CSV 输出前提醒。取消/失败/重试/手工路径的关键设备交互补验；已有 VM 测试无需无故重复扩大。
6. 临时页已加“WebP 边界”按钮（还没点）：有损/无损/透明/EXIF、动画拒绝、截断/坏压缩数据。真实设备结果确认后保存小摘要，勿把 host Pillow 替身当真机。
7. 分享图的 PNG 像素/视觉、产品页保存按钮和小红书模板；最新编辑/库存界面亲自视觉 QA。用户审美验收继续单列。
8. 补最终验收记录和许可目录 README；比较生产账本前后未变，部署正常生产键版本给用户。当前手机仍是隔离测试版。
9. 差异自查 → commit/push → 创建 PR 并 attach_artifact。尚有真实失败时不提“已验证完成”的 PR。无需等正式发行或自行冒充用户审美通过。

## 手机、隔离数据与构建操作

- Linux 主机 spark-5681，Ubuntu 24.04 ARM64，Node 24.19。
- Xiaomi 14 Ultra / 24031PN0DC，Android 15 SDK35，WebView Chrome131.0.6778.260。
- ADB：`/home/kenbattle/.local/bin/adb`；当前 Wi-Fi serial `192.168.0.5:32799`，已配对。若端口改变，用 `avahi-browse --resolve --terminate _adb-tls-connect._tcp` 找已配对服务，不重复索取配对码。
- 官方标准调试基座 `io.dcloud.HBuilder`，版本 5.26 / 15.26。远程装 APK 曾被小米 SIM 限制，用户已手动安装这个基座；无需再走安装绕路。正式包名/签名另见 `docs/qin-apk-handoff.md`。
- 生产存储 key：`pindou.ledger.v1`。操作前原样备份在 `/tmp/pindou-issues789-20260922/device-test/user-ledger-cdp.json`（初始空库存、seq0、epoch1）。**不要把测试数据写入该 key。**
- 测试 key：`pindou.acceptance.789.20260922`；测试 DB `pindou_acceptance_789` / `_doc/pindou_acceptance_789.db`。临时 prepare-build.py 复制源代码后仅改这些目标，再加测试页；不修改生产存储定义。
- 交接已停止本次临时资源同步进程（session 16826），没有停止手机应用或清空任何数据；接续需要重新启动同步。设备当前仍显示隔离测试页。

构建正式源码：

```bash
npm run build:app
```

构建并同步隔离版（在主仓库运行，网络/ADB/编译按沙箱规则申请执行权限）：

```bash
python3 /tmp/pindou-issues789-20260922/device-test/prepare-build.py
UNI_INPUT_DIR=/tmp/pindou-issues789-20260922/device-app UNI_OUTPUT_DIR=/tmp/pindou-issues789-20260922/device-dist node_modules/.bin/uni build -p app
node /tmp/pindou-linux-build-20260922/launch-douji.cjs 192.168.0.5:32799 /tmp/pindou-issues789-20260922/device-dist
```

输入/输出目录必须绝对路径，否则曾漏掉静态资源。同步脚本运行至 45 分钟或 Ctrl-C，只开本次临时服务。

设备工具都在 `/tmp/pindou-issues789-20260922/device-test/`：

- `cdp.mjs`：自动查应用 PID、forward 9223，`PAGE=pages/acceptance` 或 `PAGE=pages/pattern/edit` 选实际页面，参数为 JS 表达式，`OUT=/tmp/...json` 保存结果。目标不存在或异常会失败。
- `tap.mjs`：按 uni-button 精确文字滚动并发送 CDP touch，依赖前一步已正确 forward9223。普通 DOM `.click()` 对部分控件不可靠，不能把它没反应当应用故障。
- `eval-file.mjs`：读取 JS 文件后交给 CDP，避免复杂 shell 转义。
- `acceptance.vue`：仅隔离副本使用，按钮包括系统选图、WebP 边界、18 张批次、148识别、部分图例、导出/恢复、分享。不要提交此临时页。
- `NativeProbe.java`/`probe.jar` 是受限的 DocumentsUI 辅助；小米禁止输入注入，系统文件点击未可靠成功，不再围绕这个反复尝试。不要进入微信目录/界面。
- 原图选择目录 `/sdcard/Download/pindou-test789/`，其中 S04.webp 可用于原字节核对；148 图为 S18.png，部分图例为 S03.webp。

若用户把应用切到后台，CDP 可能挂起，不能立即归因代码。仅恢复自己应用：

```bash
/home/kenbattle/.local/bin/adb shell am start -n io.dcloud.HBuilder/io.dcloud.PandoraEntry --es appid HBuilder
```

`PandoraEntryActivity` 不导出，直接 am start 会失败；使用上面的正常入口。

## 算法、许可证与最终交付说明

- 算法 `pixel-glyph-0.8-dev`；本轮未在手机适配时改算法结果。255 图评分历史在 `experiments/issue8/review-v08.md`：foundation17/18（S03不完整未过）；旧37中30/37、有效221内30/30；旧100中71/100、有效221内71/71；最新100中37/100、有效221内37/37。最新100输出97 partial、3 failed、0 ready，**不能把有效候选准确理解成完整图纸覆盖率**。
- 源码和约 620.5KiB 字形资源随包；27MiB 左右 Pyodide314.0.7/NumPy2.4.6/OpenCV4.11.0.86/Pillow12.2.0 由 `scripts/prepare-recognition.mjs` 固定版本构建时下载，`generated/` 忽略。
- `app/static/recognition/licenses/` 已有许可全文，但 README 仍是临时研究报告，错误列出未保留的 package.json/METADATA 副本；需要改成简洁分发说明。只读代理笔记在 `/tmp/pindou-issues789-20260922/license-summary-2026-09-22.md`，不是可直接复制的成品。保留官方版本/源码链接；字体通知目前在 experiments/issue8/font-notices.txt，可随模型许可一起携带。不要扩成无关全量审计。
- GitHub 插件可查 issue/建 PR；若无法推送本地提交图，先说明工具缺口，再用原生 git push。已有 gh 凭据可用：`git -c credential.helper= -c 'credential.helper=!gh auth git-credential' push ...`。不要输出凭据。
- PR 创建后必须 `codex_app__attach_artifact`；不要擅自 merge/关闭与当前未通过条件不符的票。最终报告区分真实设备验证、自动化、自行复核与用户验收。
