# 拼豆库存与图纸消耗软件需求书（v1.0）

日期：2026-09-15。状态：需求已确认，可进入开发设计。

## 1. 产品目标与范围

帮助个人拼豆爱好者回答三个问题：每种颜色还剩多少颗；一张图纸分别需要多少颗；按现有库存制作后，哪些颜色不足、预计剩余多少。首版为个人使用的 Android 手机应用，先单设备保存库存；后续开发微信小程序并增加同步。断网时须能查看库存、手工更正及标记“已拼”并完成对应本地扣减；新截图的图例自动识别仅要求联网可用，不要求离线识图。首版不擅自承诺账号或云同步，但应考虑后续数据迁移。

首版的成功标准：使用用户提供的 MARD 221 色卡录入库存后，能对一张受支持的图纸生成逐色需求、总需求、库存缺口和预计余量；确认制作后能准确扣减库存，并可追溯和撤销误操作。

## 2. 用户与核心场景

用户为持有多色拼豆、按图纸制作作品的个人。典型流程：初始化色卡 → 盘点并录入库存 → 导入图纸或手工录入用量 → 核对颜色映射与数量 → 查看能否制作及补货清单 → 确认制作并扣减 → 后续入库、盘点或更正。

“库存”指当前实际可用的颗数；“图纸需求”指该作品各颜色所需颗数；“预计余量”仅用于预览，不等于已扣减库存。用量和库存均以颗为单位。首版库存按 MARD 221 色号汇总，不拆分品牌、尺寸或批次；不以屏幕显示的 RGB 值当库存主键。开封后的数量允许按颗估算；估算与精确盘点应在界面上有所区分。

## 3. 首版功能需求

### 3.1 色卡管理

- 首版采用用户提供的[MARD 221 标准版拼豆色卡示意图](./参考样例/MARD221色卡-用户提供.png)的色号体系，共 9 个色系、221 个色号；先前用户写的“211 色”已确认是笔误。每色至少有唯一色号、所属色系及软件内部色卡标识。图中没有逐色 HEX/RGB 数值。经[来源核验](./research/MARD221-色值与版本来源.md)，本轮尚无可核实的品牌方完整 221 色数值表及官方版本记录。用户决定首版先采用所附[221 色社区来源 CSV](./research/MARD221-参考色值-社区来源.csv)中的 HEX/RGB 作为全应用统一的“屏幕参考色”，在色卡和库存等颜色展示处明确标注“社区版参考色”，同时始终显示色号；附件有 221 条、9 组数量匹配、无重复色号，HEX 与 RGB 一致。软件内部将这套显示资料标为“MARD 221／社区版参考色”，不填写或虚构 MARD 官方版本号。不得将该 CSV、截图取色或第三方颜色标为“MARD 官方精确色值”。将来取得可追溯品牌方原表并核验后，可更新界面展示色；旧图纸、制作记录、色号、用量、库存扣减及其当时的社区版来源记录均保持不变。
- 色号范围：A1–A26（黄橙系，26）；B1–B32（绿色系，32）；C1–C29（蓝青系，29）；D1–D26（蓝紫系，26）；E1–E24（粉玫系，24）；F1–F25（红色系，25）；G1–G21（棕肤系，21）；H1–H23（黑白系，23）；M1–M15（大地系，15）。合计 221。以上名称和数量依据用户提供的示意图，不代表已经核验官网版本。
- 色卡作为首版库存和用量的统一色号依据；用户当前只使用 MARD 221 色号，首版同色号库存可以合并记录，不另拆品牌、尺寸或批次。其他品牌/尺寸的将来扩展需重新定义合并规则。
- 用户表示原始图纸不会使用 MARD 221 色卡外色号；识别出的色卡外色号不得进入正式逐色用量，但必须在确认页面显示被拒绝项及原始识别内容，不得静默丢弃。用户允许看过提示后不处理该项、继续确认；此时该项仍不计数，存在有效色号被误读后漏算的风险。确认页面必须清楚显示“未计入”的色号和数量，而不能把它混入正式总数。此规则已获用户在风险样例下确认。

### 3.2 库存管理

- 按色号查看当前库存、低库存提示；支持逐色录入、批量盘点、采购入库、制作消耗及库存更正。首次盘点须提供适合手机的可搜索 MARD 221 色号批量清单，能连续填写多个色号当前颗数；未填写色号仍为 0。库存输入单位统一为颗，估算/精确状态逐色标记并在清单中可见；设定新总数时默认沿用该色号原标记，可逐色改动，只有用户明确设为精确才移除估算。估算余额经历制作扣减后仍为估算。补货直接输入该色号“现在共有多少颗”：新余额高于旧余额时记补货差额，低于旧余额时提示改用盘点更正，不将减少量伪装成补货。首版不做包装包数换算。
- 每次实际库存变化记录时间、色号、变动数量、原因、关联作品/图纸及变动前后数量。误点“已拼”允许撤回任意一条制作记录，仅恢复该条记录对应的逐色扣减；发现账面与实际不符时允许按色号盘点手工更正。撤回与更正应可追溯，不直接抹除原变动。
- 批量盘点/补货统一保存前展示本次变更，保存须整批成功或整批失败；不逐色边输入边生效。补货批次若含新余额低于旧余额的色号（例如 A1 500→800、B1 300→250），保存前指出该色号并暂不保存整批；减少项须另走盘点更正，不能静默只保存增加项。首版不提供整批库存录入撤回；若录错数，可再做批量盘点更正，保留原操作与更正痕迹。仅更改某色号“估算/精确”标记，即使颗数不变也应能保存，并记录标记变化。
- 不允许库存变成负数。数量不足时显示逐色缺口并阻止该次“已拼”扣减；即使作品实际上已经完成，也须先手工更正账面库存至足量，再完整扣减图纸用量，不能只扣账面现有数量。
- 首版必须能由用户手工导出完整数据备份并从备份导入恢复，以应对卸载、重装或换手机；不做自动导出或备份提醒。恢复前预览现有数据与备份的差异，由用户明确选择整体替换或取消，不自动合并两份库存/流水。备份保留图纸缩略图和已确认用量，不保留原始大图；缩略图只用于辨认图纸，不要求放大读出逐格色号，具体文件格式待定义。首版不做逐色库存 CSV 导入；完整备份导入只用于数据恢复/迁移，不是日常库存批量录入。

### 3.3 图纸与用量

- 用户提供的典型[豆画 / Mard (148) 图纸样例](./参考样例/豆画-Mard-148图纸样例.png)：29 列 × 16 行坐标网格，制作格内直接标 MARD 色号，底部有逐色数量图例。图例为 C2=18、C12=3、C16=11、C17=12、C19=7、C29=18、H2=14、H3=5、H7=60；图例求和 148 颗。该样例显示 H2 白色格也需要计数，而未标色号的棋盘格不应仅因外观接近白色就算作 H2。
- 每张图纸保存名称、来源文件/备注、尺寸、色卡版本、逐色需求及处理状态。
- 首版输入确定为 PNG/JPG 图纸截图；用户表示日常图纸每张都有底部逐色数量图例。首版以图例为用量的主要数据源，不要求自动逐格复算。导入后必须展示识别出的各色数量和总数，允许逐色修改、删除错误项或新增漏项；总数应由确认后的逐色数量求和。用户确认后才可用于库存对比和扣减，截图自动识别不得绕过确认步骤。
- 若整张底部图例自动识别失败，首版必须允许用户从 MARD 221 色卡完整手工建立逐色数量，并明确标示“手工录入”，仍经过逐色用量确认。若仅识别出部分图例，须明确标示“部分识别”，展示已识别项目，允许用户逐色修正、删除或新增漏项后再确认，不能假装识别完整。断网时也可选择 PNG/JPG 图纸截图、完整手工录入逐色用量并保存；自动识别仍只在线可用。自动识别首版优先保证用户提供的“豆画 / Mard”这类清晰坐标网格、底部横排色号数量图例的版式；其他版式不承诺自动准确，可走手工流程。结构化图纸导入和普通照片转网格留待后续评估。
- 样例中每个明确标有有效色号的制作格对应一颗豆；未标色号的格子不计数，包括未标号的棋盘格背景。首版不依赖网格 OCR 来生成用量；确认后的逐色数量之和为总需求。若能读到截图标题总数，且其与逐色求和不一致，必须显示差值并要求用户再次明确确认；允许确认后的逐色数量作为库存计算依据，不强制采用标题数字。
- 导入前显示截图预览、有效逐色项目、被拒绝识别项和统计结果；用户确认后才保存需求。首版只按 MARD 221 色号记录图纸用量，其他色卡映射不是本阶段已确认的需求。
- 同一图纸可以先导入并多次预估；半成品不单独扣库存，只有用户在实际拼完后手动点击“已拼”才按整张图纸用量扣库存。首次“已拼”后按钮变为“再拼一次”，用户明确点击后可为另一件实际完成的作品新增制作记录并再次扣减。每条制作记录固定当次已确认的逐色用量快照；后续修改图纸只影响新制作，不自动改动旧记录。误点可撤回该次记录并恢复库存。一次点击连点或重试不得造成重复扣减。
- 在线图例识别仅在用户主动点击识别当前 PNG/JPG 截图时发送该截图给在线处理服务；首版不上传整份库存、制作记录或盘点历史。在线处理完成后须删除服务端截图，不长期留存；选择识别服务时必须核验其删除能力与实际处理流程。

### 3.4 消耗预测与扣减

对每个色号 c：需求量 `need[c]`；当前库存 `stock[c]`；缺口 `max(need[c] - stock[c], 0)`；预计余量 `stock[c] - need[c]`（不足时显示负值或以“缺口”呈现，不写入负库存）。只统计图纸里出现的颜色，同时提供全色库存视图。

系统显示总需求、涉及颜色数、足量/不足颜色、逐色缺口及可导出的补货清单。导入、确认图纸用量、预估及半成品状态都不改变库存；用户实际拼完后点击“已拼”或明确点击“再拼一次”，立即按当时已确认的逐色需求扣减，不弹第二次确认。损耗不单独记录；实际余量与账面不符时可盘点更正。不足时提示缺口并阻止扣减，不生成负库存；先更正至足量后才能完成扣减。

## 4. 首版页面与模块

色卡/库存总览（搜索色号、筛选不足）；库存录入与变动记录；图纸列表与导入预览；单张图纸的逐色需求及库存对比；“已拼”与历史记录；数据备份/导出。首版 Android 应用优先适配手机触屏，平板为适配目标；需验证系统相册/文件选择、预览与本机保存。

## 5. 数据概念与关键约束

| 对象 | 最小字段 | 约束 |
| --- | --- | --- |
| 色卡版本 | 版本标识、来源、启用时间 | 一个版本内色号唯一；旧图纸保留原版本 |
| 颜色 | 色号、所属色系、展示色值、展示色来源、色卡内部标识 | 社区版参考色与官方色值来源分开；展示色值不作为库存主键 |
| 库存余额 | 色卡版本、色号、颗数 | 颗数为非负整数 |
| 库存流水 | 时间、色号、变动值、原因、关联记录 | 真实变动可追溯 |
| 图纸 | 名称、来源、尺寸、色卡版本、状态 | 原始文件可选；需求可复核 |
| 图纸用量 | 图纸、色号、颗数 | 颗数非负；总量为逐色求和 |
| 制作记录 | 图纸、确认时间、扣减快照、状态 | 同一制作请求最多扣减一次 |

完整备份至少保留色卡版本、逐色余额及估算标记、库存变动历史、图纸已确认用量、制作记录及撤回/盘点更正历史，以及仅用于辨认图纸的缩略图；不包含原始 PNG/JPG 大图。后续微信小程序应能接续这些数据；首次迁移由用户从 Android 应用手工导出备份，再在微信小程序导入，云端多设备同步后续另做。实际文件选择与解析方式须在微信真机验证。

## 6. 非功能要求与验收样例

- 计算应可重复验证：同一图纸、同一色卡与同一库存快照，输出一致；库存发生变化时预估实时更新。
- 导入失败不能改动库存；确认扣减须整体成功或整体失败，不能只扣其中几个颜色。
- 本地数据需有明确的导出与恢复途径；文件导入要限制格式和尺寸并给出可理解的错误信息。
- 无障碍基础：颜色不作为唯一信息，始终显示色号与数字。
- 自动识别验收：对项目保存的“豆画 / Mard (148)”原始样例截图，在联网且无需用户手工修正的情况下，须识别底部图例全部 9 个色号及对应数量：C2=18、C12=3、C16=11、C17=12、C19=7、C29=18、H2=14、H3=5、H7=60，自动求和为 148。任何漏项、错号或错数，即使用户随后能修正，也不算该样例的自动识别通过。

验收样例：库存 A1=100、B1=10；图纸有效格 A1=80、B1=15、空白=5。结果为总需求 95、A1 余量 20、B1 缺口 5；预览不改变库存，因 B1 不足而阻止确认制作。补货时把 B1 当前总数设为 15（历史记补货增加 5）后确认制作，余额 A1=20、B1=0；再次提交同一制作请求不再扣减；撤销产生反向流水并恢复余额。

## 7. 分阶段规划

- 阶段 1（MVP）：交付个人 Android 安装包；内置已确认的 MARD 221 色号集合及附件社区屏幕参考色（界面标注“社区版参考色”）、库存录入/流水、在线 PNG/JPG 截图图例识别及用量确认、部分识别提示与完整手工录入、离线选择截图并手工录入/保存图纸、离线库存查看/手工更正/“已拼”、用量对比、缺口与扣减、完整数据导出/恢复。提供的 148 颗清晰样例须无需手工修正地自动识别全部 9 项。
- 阶段 2：普通图片转网格、人工校色、尺寸与调色策略、多个品牌色卡/映射、采购统计。
- 后续阶段：微信小程序先支持手工导入 Android 备份，再增加账号、多设备同步与云端备份；用户已明确后期需要同步，隐私与成本须在该阶段确认。

## 8. 已知边界与后续变更

- 首版使用的颜色块为“社区版参考色”，而非 MARD 官方精确色值；官方原表仍需在未来取得可追溯材料后另行核验。
- 更换显示色仅影响之后的界面展示，不会改写历史色号、颗数、库存流水、制作记录或其当时的色值来源。
- 首版不拆分品牌、尺寸或批次；若未来增加其他豆种，需要重新定义库存维度。
