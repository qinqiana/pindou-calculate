# 拼豆根 spec：产品细节直接确认记录

**用途：** 与用户逐项敲定影响实际使用的产品细节，答复回填 [SPEC.md](./SPEC.md)。

**确认人：** 用户（产品负责人）。技术实现由主会话决定，不再要求用户回答框架、数据库、文件编码、平台 API、测试方法或证据如何获取。

## 已明确的责任和规格

- 用户决定产品边界、体验、审美和实质费用取舍；主会话负责技术方案、实现与验证。
- 已有 Android 真机和 Windows 开发设备；不要求纯 Linux 编译。
- 小红书全屏图比例由用户明确为 **9:15**，与 3:5 等价；本项目默认输出 1080×1800 px。编码、压缩和安全区由主会话验证，不再重复问比例。
- 首版在线识别和处理后删除的边界保留。服务选型由主会话处理；确有费用或持久部署需求时，提供具体方案再确认。
- 产品细节已敲定，用户随后授权将根规格和技能配置提交到 GitHub；本次不实施应用代码。

## 1. 低库存预警

**用户已答：** 用户可以自己设置比例，比如 5%、10%、20%。

**已记入：** 采用自设百分比，不再采用固定颗数阈值，也不限制只有三个选项。

**用户已确认基准：** 首次录入或最近补货后的该色数量；扣减、撤回和盘点更正不重置。

例如基准 1000 颗，设为 10%，剩余不超过 100 颗提示。基准及预警比例随备份保留；零或未知基准由实现安全处理，不虚构百分比。

## 已确认的其他产品细节

### 2. 分享卡的主要内容

**用户已答：** 这个先留空，默认展示作品和图纸。

**已记入：** 详细信息与排版留到视觉稿，先前建议的名称、用量数字和制作状态不列为必做字段。小红书 9:15 已确认；像素与安全区由主会话负责。

### 3. 首次库存输入的默认数量与精度

**用户答复：** 前期默认 1000，后期增加厂商支持，根据厂商录入数量。

**用户已确认生效方式：** 录入时预填 1000，用户确认持有后才计入库存；未录入仍为 0。批量保存只提交用户确认的色号，展示预填不触发全部入账。

精度标记由主会话采用安全默认：预填数按估算处理，用户明确选择精确才标精确；不把用户的数量答复记成其已经确认实际点清。厂商支持为后续需求，不扩大首版 MARD 221 的已定范围。

### 4. 全零用量图纸与已使用成就

**用户已答：** 可以已拼。耗材管理同时记录余量和已经使用的，使用也是一种成就。

**已记入：** 零用量图纸可已拼，记录一次完成；库存不变、已记录使用颗数增加零。撤回对应制作时，移除其已用统计和完成计数。旧制作快照不受后续图纸修改影响。

**用户已确认：** 首版先做累计已用总颗数、各色已用颗数和已完成作品数；只汇总未撤回制作记录，盘点更正不自动算成制作消耗。

## 主会话负责的事项

采用 uni-app / Vue 3 / TypeScript、Android SQLite 事务、共享业务规则、版本化 JSON 含缩略图备份、统一在线识别合同。识别重复候选保留原文并进入现有用量确认流程，不自动累加。测试、容量验证、文件路径、分享编码、权限和广告/权益能力边界由主会话落实。

技术选择不代表能力已经实测。若出现会改变已确认功能、数据安全、范围、费用或发布行为的真实取舍，主会话带具体方案再问用户，不用一长串技术问题代替自己的调查。

## v1.0 确认状态（历史记录）

本轮问题已回答。分享的详细信息和排版按用户要求留到视觉设计阶段；无待用户填写的技术题。用户已授权将根规格发布到 GitHub；此记录作为需求来源一同保存，不表示已实施应用。

## 2026-09-15：v1.1 修订基准

**来源：** [老板的五项反馈](https://github.com/qinqiana/pindou-calculate/issues/1#issuecomment-5682310424)，以及用户转述的网格定位补充意见。用户强调“技术实现路径不是绝对的，核心是结果”，随后同意以讨论后的要求为基准修改 spec，并要求在开发原则中写明满足需求优先，不为实现特定技术而阻塞。

**本次回填：**

- 图纸本体识别以逐色用量正确、统计区域完整、疑点可核对和修正为要求；作为后续能力目标，支持边界和排期须经样例验证及阶段确认，不强制独立网格定位界面、全部格线恢复或人工校准。
- 用量逐项可改，通过一个主按钮整体确认；确认用量不扣库存，已有异常提示、知情确认和旧制作快照规则保留。手工用量不以 OCR 失败为前提。
- 按反馈取消“根据厂商预设录入数量”的后续计划；首次预填 1000、按 MARD 221 色号记账的规则保持。
- 开发以满足已确认需求和通过实际验收为先，技术路线可在授权范围内调整；功能、数据保护和必要验收不能因此降低。

**仍待确认：** 首版是否后移 OCR，以及是否同时后移图纸/分享；未取得可核验官方色值时采用什么显示方式。此前提出的这两项选择尚未得到明确答复，当前仍执行原首版功能和社区版参考色方案，详见根规格 D12、D13。技术引擎和算法由主会话负责，不作为产品选择题。

本轮为本地文档修订；上文 v1.0 的答复保留作历史来源，本节明确的修订以根规格 v1.1 为准，不表示已完成应用开发或真实识别验收。

## 2026-09-15：v1.2 分阶段拆票确认

用户要求使用 to-tickets，尽量拆细并补全实现细节；随后明确“第一阶段交付安卓，然后才是小程序和外接，不要混淆阶段目标”。针对外接是否包括在线 OCR 的直接问题，用户回复“包括，但是可以探索，是否有不适用ocr的办法”；按上下文记为允许探索不使用文字识别的计数方法。

已回填根规格：第一阶段以 Android 本地库存、手工图纸用量、制作/撤回、使用成就、完整备份恢复、本地双模板图片和签名安装包为完整交付。小程序运行时、在线 OCR 与其他外接后移；非文字识别探索单列且不阻塞 Android。技术路径按实测结果选择，探索中的自动结果和人工映射/修正分开记录，不因此增加首版自动识别承诺。

本次确认取代 v1.1 中 D12 的待定状态及更早的首版在线识别/微信验证前置条件；D13 色值显示选择仍待确认，现有社区版参考色方案继续有效。正式拆分稿待用户确认粒度和依赖后再发布 tickets，本次不修改父 Issue。

用户随后补充：云 OCR 有个人免费额度，Spark 也可作为开源 OCR 的本地部署候选；核心是已知图纸点阵时，本地算法是否足以计数。已按此收紧探索目标，不把 OCR 调用成本当作决定技术的前提。2026-09-16 完成一张原始 148 颗图的只读实验：点阵和每色代表格的色号人工给定，九色数量全部正确，无 OCR/网络调用；自动定位、自动识号、其他图纸、Android 及 Spark 部署均未验收。具体证据与免费额度的一手资料核验见拆分稿附录。

## 2026-09-16：同步根 Issue 与独立审查

用户明确要求把根 Issue #1 更新到 v1.2，并指定 K3 review 43 张草稿，说明无法逐票审阅。此次明确授权取代上轮拆票过程“不修改父 Issue”的操作约束；根规格正文与仓库来源同步，父项保留开放状态及原讨论。K3 独立审查需求、粒度和依赖，主会话核实并处理确定问题，只向用户汇总必要决策。此次更新根 Issue 和审查草稿不等于创建 43 张实施 Issue。
