# 0.2 基础算法复盘与自行复核

2026-09-20；本地未提交、未推送。主会话自行复核，未做独立 QA。K3 前期进行了原实现审查及人工抄录，不能算当前新增实现的独立 QA；Luna max 按用户要求只收集新30图。

## 已确认根因与处理

| 原问题 | 实际影响 | 本轮处理与证据 |
| --- | --- | --- |
| 只识别同条括号数量 | 左右/上下图例全部丢失 | 同一自动入口识别三种排列，S06/S09/S10/S11/S12等无需手工裁框 |
| 原图文字裁框过窄 | 三位色号末字被截，M13等变成别号 | 扩大相对字框，保留原图坐标；S09/S10逐色全等 |
| 固定阈值、大字模板 | 点阵4被读成N/H，浅字消失 | 局部对比度、多阈值字形证据与约435 KiB小分类器；无原图训练 |
| 连通组件直接当字符 | 断笔多字、粘连少字 | 同列断筆合并、有限候选切分；S05的54色完整返回 |
| 单个小字决定色号 | C6/C8、C20/C28、H/M系统混淆 | 在同图格内多位置读取并保留投票；同图字形只修正候选且显式提示 |
| 仅小字投票、颜色近似 | 两个独立图例项可能被误合成E16 | 冲突不允许合并；回归保留独立E16与H22。RGB不作为正式色号依据 |
| 新数量字段忽略小符号 | `-3`/`1.5`误变正整数 | 三种图例均有变造原图测试；符号跨阈值保留，断笔内部不当小数点 |
| 全幅重复像素缓冲 | 约404→277 MiB，仍偏高 | 128行预览和局部像素读字，原图一份；当前逐图最高约216 MiB；训练结束后148图三次新进程中位203.14 MiB |
| “程序运行了”代替正确结果 | 1/18被包装成基线完成 | 建立参考答案隔离的评估器，错号/漏色/多色/重复/超容差全部失败 |

## 验收证据

- 基础完整分母18，15通过；14张所有数量精确相同，S13的C7=88（参考80）、H5=185（参考195）在已确认容差内。
- S03截断；S04的H6/H7被水印遮挡；S15的F8数量被遮挡。没有完整参考答案，三张均不计通过，不由标题补数。
- S00色卡拒绝。原148图九色、316空白、0未知仍成立；隐藏图例派生图继续走本体计数。
- 10项图像回归通过，新增验收器检查覆盖误差边界、错漏、重复和无完整真值。所有测试只验证实验入口，不假称Android或用户验收。
- `recognize.py`、布局识别、字形推理、本体交叉识别不读取样例清单、参考答案、文件名名单；评估器在子进程完成后比对参考。
- 训练只有字体生成字形；训练环境复用现有PyTorch；推理模块未导入PyTorch。训练源文件、种子、增强范围、权重均可复查。
- 冻结目录位于`/tmp/pindou-issue8-v02/frozen-foundation`。收集新图前建立；冻结源码与资源逐文件直接比较相同，没有计算哈希。评估器后来增加外部manifest参数，不改变冻结识别器。

## 未解决边界

- 新三版式仍返回`partial`，候选正确不意味着整图覆盖已经自动证明，也没有品牌验证。标题总数尚未自动读出。
- 几何规则假定正交、近规则格线及图例成排；没有任意透视/手拍图承诺，真正无图例泛化尚缺证据。
- 字符分数来自有限字体训练，未校准为正确概率；字段语法候选与同图修号均保留原文和疑点。
- Python全图解码、模板旧路径及两模型目前同时保留，仍有精简空间。按用户最新要求，先完成新30图验证，再删减/移植并重新验证。
- 小图例交叉识别最多每色读25格、网格上限16000格；这个上限控制本地实验耗时，不代表完整逐格计数。旧独立本体路径最多4096格。
- 未接Android，未测APK包体、手机RSS、电量、取消/换图保护或真实用量确认总成本。

新30图的结果、来源与参考复核另记；当前15/18不外推泛化准确率。
