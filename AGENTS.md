# 拼豆项目

开发、拆分任务或验收前，读取 [SPEC.md](SPEC.md)。它是当前根规格；原需求书及问答记录保留作来源，后续确认的变更回填根规格。

## 协作与决策

- 本仓库按多人开发协作；产品是否支持多用户仍以根规格为准。
- 并行任务明确负责人、目标版本、修改范围和验收条件。各自使用独立分支；同机并行使用独立工作目录或 worktree，避免共享未提交改动。小修不强制另开 ticket。
- 开始前检查已有改动，提交前检查差异，只提交本任务内容；保护其他人的工作，不擅自覆盖、回退或清理。合入由任务约定的集成人负责，代理协作由主会话整合。
- 修改共同依赖的数据结构、备份格式或公共接口前，先检查真源与调用方，由主负责人协调约定，再分工实现；避免多人同时改同一真源。
- 产品方向、体验和实质规则冲突交用户裁决；已授权的技术实现由主负责人决定。冲突只暂停受影响部分，确认结果回填根规格。

## 工作区卫生

- 版本控制保留源码、正式文档和必要测试素材。采用实际工具链时补齐最小 `.gitignore`，排除构建产物、缓存、日志、本地配置和凭据。
- 临时文件使用已有任务目录；没有约定时放 `/tmp` 下的本任务目录，不散落仓库根目录。只清理本任务明确产生的临时内容，保留历史需求及原始素材。
- 交接写明完成内容、验证结果、剩余问题及需保留的本地改动，不以工作区干净代替任务完成。

## 版本与范围

- 拆分 tickets 前，须取得用户对[根规格 Issue #1](https://github.com/qinqiana/pindou-calculate/issues/1) 的明确确认；发布规格或标为 `ready-for-agent` 不替代这次确认。
- 每次开发明确目标版本的目标、必做项、不做项和验收条件，范围回查根规格的阶段边界。规格文档版本、应用发布版本和备份格式版本分别管理。
- 后续候选不自动进入当前版本。只实现当前闭环和已确认的必要接口；真实需求到来时再增加框架、SDK、服务或治理规则，不为未来提前搭空架子。
- 优先复用已有实现及平台能力；精简不得削弱数据保护、输入校验、必要错误处理、无障碍基础或已确认验收要求。

## 验收与发布责任

- 完成以根规格中的功能闭环和对应证据为准。区分静态检查、常规测试、真实平台运行、独立 QA 与用户验收；文件存在或浏览器预览不能替代 Android 验收。
- 每次代码交付前进行独立新会话 QA，交接已确认需求、改动与证据，不继承实施推理，不使用 Luna 承担 QA。主会话亲自检查视觉产物，再交用户验收。
- 修改持久化数据或备份格式时，明确旧数据兼容及失败保护，不静默清空用户数据。首次打包或接入服务前，明确签名密钥、凭据及发布权限的保管人；凭据不进入仓库，公开发行另行取得授权。

## Agent skills

### Issue tracker

需求和规格使用本仓库的 GitHub Issues；操作前读取 [issue-tracker.md](docs/agents/issue-tracker.md)。

### Triage labels

使用五个标准分流标签；分流或修改标签前读取 [triage-labels.md](docs/agents/triage-labels.md)。

### Domain docs

采用单一领域文档；调查概念、架构或验收规则前读取 [domain.md](docs/agents/domain.md)。
